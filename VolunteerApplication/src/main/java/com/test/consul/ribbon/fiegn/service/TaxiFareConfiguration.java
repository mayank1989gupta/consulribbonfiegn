/**
 * 
 */
package com.test.consul.ribbon.fiegn.service;

/**
 * @author MGupta
 *
 */
//@Configuration
//@ComponentScan("com.test.consul.ribbon.fiegn.service")
public class TaxiFareConfiguration {
	/*
	 * private static final String drlFile = "TAXI_FARE_RULES.drl";
	 * 
	 * @Bean public KieContainer kieContainer() { KieServices kieServices =
	 * KieServices.Factory.get();
	 * 
	 * KieFileSystem kieFileSystem = kieServices.newKieFileSystem();
	 * kieFileSystem.write(ResourceFactory.newClassPathResource(drlFile));
	 * KieBuilder kieBuilder = kieServices.newKieBuilder(kieFileSystem);
	 * kieBuilder.buildAll(); KieModule kieModule = kieBuilder.getKieModule();
	 * 
	 * return kieServices.newKieContainer(kieModule.getReleaseId());
	 * 
	 * }
	 */
}
