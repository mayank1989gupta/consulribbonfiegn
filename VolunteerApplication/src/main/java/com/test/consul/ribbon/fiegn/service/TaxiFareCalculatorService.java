/**
 * 
 */
package com.test.consul.ribbon.fiegn.service;

import org.springframework.stereotype.Service;

/**
 * @author MGupta
 *
 */
@Service
public class TaxiFareCalculatorService {

	/*
	 * @Autowired private KieContainer kContainer;
	 * 
	 * public Long calculateFare(TaxiRide taxiRide, Fare rideFare) { KieSession
	 * kieSession = kContainer.newKieSession(); kieSession.setGlobal("rideFare",
	 * rideFare); kieSession.insert(taxiRide); kieSession.fireAllRules();
	 * kieSession.dispose(); System.out.println("!! RIDE FARE !! " +
	 * rideFare.getTotalFare()); return rideFare.getTotalFare(); }
	 */
}
